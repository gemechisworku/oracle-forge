"""MongoDB specialist — aggregation pipelines via MCP Toolbox."""

from __future__ import annotations

DATABASE_KEY = "mongodb"


def describe() -> str:
    return "MongoDB aggregation pipelines through MCP Toolbox."
